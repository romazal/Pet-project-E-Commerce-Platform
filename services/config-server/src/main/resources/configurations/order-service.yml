spring:
  datasource:
    url: jdbc:postgresql://localhost:6543/order
    username: romazal
    password: 123456
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 4050

application:
  config:
    payment-url: http://localhost:4060/api/v1/payments
    product-url: http://localhost:4040/api/v1/products
    customer-url: http://localhost:4020/api/v1/customers
    shipping-url: http://localhost:4070/api/v1/shippings

#logging:
#  level:
#    root: DEBUG